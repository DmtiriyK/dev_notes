# 🧭 Управляющие конструкции в Python — по красоте

Синтаксис                            | Функционал
====================================|=======================================================================
if / elif / else                    | Условия  
                                   | ✅ `if x > 10: print("больше 10")`  
                                   | elif = "иначе если", else = "иначе"

for ... in ...                      | Цикл по последовательности (строка, список, range)  
                                   | ✅ `for i in range(3): print(i)`  
                                   | Печатает: 0 1 2

while условие                       | Пока условие выполняется — повторяй  
                                   | ✅ `while x < 5: x += 1`

range(start, stop, step)           | Генератор чисел  
                                   | ✅ `range(1, 10, 2)` → 1, 3, 5, 7, 9  
                                   | ✅ `range(5)` → 0 до 4

---

## 🧃 Управление циклом

Команда     | Действие
============|==================================================
`break`     | Выйти из цикла досрочно  
`continue`  | Пропустить текущую итерацию и перейти к следующей  
`pass`      | Ничего не делает — просто заглушка

---

## 📌 Примеры

```python
# if/else
x = 5
if x > 0:
    print("Плюс")
else:
    print("Минус")

# for + range
for i in range(3):
    print(i)

# while
x = 0
while x < 3:
    print(x)
    x += 1

# continue
for i in range(5):
    if i == 2:
        continue
    print(i)
