# 📊 Агрегатные функции в SQL

Синтаксис               | Функционал
========================|========================================================================
COUNT(*)                | Считает количество всех строк, включая NULL  
                        | ✅ `SELECT COUNT(*) FROM orders`

COUNT(column)           | Считает количество НЕ-NULL значений в колонке  
                        | ✅ `SELECT COUNT(price) FROM orders`

SUM(column)             | Складывает все значения в колонке  
                        | ✅ `SELECT SUM(salary) FROM employees`

AVG(column)             | Вычисляет среднее арифметическое значений  
                        | ✅ `SELECT AVG(score) FROM tests`

MIN(column)             | Находит минимальное значение  
                        | ✅ `SELECT MIN(age) FROM users`

MAX(column)             | Находит максимальное значение  
                        | ✅ `SELECT MAX(price) FROM products`

DISTINCT + COUNT        | Считает количество уникальных значений  
                        | ✅ `SELECT COUNT(DISTINCT user_id) FROM logins`
